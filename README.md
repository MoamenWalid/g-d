
  # High-Conversion Homepage UI

  This is a code bundle for High-Conversion Homepage UI. The original project is available at https://www.figma.com/design/lJFVcnazzxYTVxNpH0RCDf/High-Conversion-Homepage-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # g-d
